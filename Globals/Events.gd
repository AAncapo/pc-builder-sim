extends Node

signal new_package(pckg)
signal added_item_to_inv(item)
signal removed_item_from_inv(item)
signal interaction_exited

## Web ##
signal request_accepted(post)
signal request_completed(post)
