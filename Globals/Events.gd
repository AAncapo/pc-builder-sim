extends Node

signal interact(tag)
signal init_interaction(obj)
signal stop_interaction

signal new_package(pckg)

signal new_client_request(req)
signal request_accepted(post)
signal request_completed(request)

signal cmp_mouse_selected(cmp_node)

signal cmp_installed(cdata)
signal cmp_uninstalled(id)
signal no_free_slot(cdata)
signal cmp_alrdy_installed(cdata)
signal cmp_missing

signal item_uploaded(data)
signal items_buyed()



signal move_camera_pls
